//! Windows TSF (Text Services Framework) frontend for live-henkan.
//!
//! This crate is only buildable on Windows.
//! Build with: `cargo build -p tsf-frontend --target x86_64-pc-windows-msvc`

fn main() {
    eprintln!("TSF frontend not yet implemented (Milestone 7)");
    eprintln!("This binary must be built and run on native Windows.");
}
